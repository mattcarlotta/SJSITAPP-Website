/* istanbul ignore file */
import styled from "@emotion/styled";

const AppMain = styled.div<{ stretch?: boolean }>`
  display: flex;
  flex-flow: column nowrap;
  flex: 1 1 auto;
`;

export default AppMain;
